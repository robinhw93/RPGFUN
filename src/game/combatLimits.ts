/** A queued ability or consumable counts as one player action, regardless of hits. */
export const MAX_PLAYER_ACTIONS_PER_TURN = 6;

/** Consumables share the action limit and may additionally be used only once per player turn. */
export const MAX_CONSUMABLES_PER_TURN = 1;
